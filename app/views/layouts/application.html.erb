<!DOCTYPE html>
<html>
  <head>
    <link href="https://fonts.googleapis.com/css?family=Indie+Flower|Luckiest+Guy" rel="stylesheet">
    <title>ArtiHub</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <p class="flash"><%= flash.notice %></p>
    <div class="nav-box">
      <nav>
        <%if logged_in? %>
          <p class="current-user"><%= "Logged in as #{current_user.username}"%></p>
        <% end %>
        <ul class="nav-ul">
          <% if logged_in? %>
            <li class="nav-li"><%= link_to "Home", articles_path, class: 'nav_button' %></li>
            <li class="nav-li"><%= link_to "Create New", new_article_path, class: 'nav_button' %></li>
            <li class="nav-li"><%= link_to "Pick a Tag", tags_path, class: 'nav_button' %></li>
            <li class="nav-li"><%= link_to "logout", logout_path, class: 'nav_button' %></li>
          <% else %>
            <li class="nav-li"><%= link_to 'Home', articles_path, class: 'nav_button' %></li>
            <li class="nav-li"><%= link_to 'Login', login_path, class: 'nav_button' %></li>
            <li class="nav-li"><%= link_to 'Sign in', new_author_path, class: 'nav_button' %></li>
          <% end %>
        </ul>
      </nav>
    </div>
    <div id="container">
      <%= yield %>
    </div>
  </div>
  <div class="logged-in-as">
    <!--  <hr />
    <h6>
      <% if logged_in? %>
        <%= "Logged in as #{current_user.username}"%>
        <%= link_to '(Logg out)', logout_path , class: "logout-button" %>
      <% else %>
        <%= link_to 'Login', login_path , class: "login-button" %>
      <% end %>
    </h6> -->
  </body>
</html>
